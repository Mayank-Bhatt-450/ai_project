# Chat Agent for Organisation

First Agentic chat which can answer from the ingested data and if the command is given py user then it can perform the task provided to that command 



# Setupo
```
pip install poetry
poetry install
```

# Usage 
```
uvicorn api:app --reload --port 8000
```
